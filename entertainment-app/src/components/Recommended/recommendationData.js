import data from '../../data.json';

// recommendation data would go here. json data does not have a recommendation bolean to indicate if its recommended therefore I used the whole data set
const recommendationData = data


export default recommendationData;
